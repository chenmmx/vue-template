const state = {
  cachedRoutes: [],
  skeleton: true
};

const getters = {
  cachedRoutes: state => state.cachedRoutes,
  skeleton: state => state.skeleton
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations
};
